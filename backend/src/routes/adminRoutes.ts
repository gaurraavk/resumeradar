import { Router } from 'express'; import { addQuota, logs, overview, system, updateStatus, users } from '../controllers/adminController.js'; import { requireAuth, requireRole } from '../middlewares/auth.js'; const router = Router(); router.use('/admin', requireAuth, requireRole('admin','super_admin')); router.get('/admin/overview', overview); router.get('/admin/users', users); router.patch('/admin/users/:id/status', updateStatus); router.post('/admin/users/:id/quota', addQuota); router.get('/admin/logs', logs); router.get('/admin/system', system); export { router as adminRoutes };
